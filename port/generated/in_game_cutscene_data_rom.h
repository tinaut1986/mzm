/* Generated by tools/gen_port_rom_offsets.py from include/data/in_game_cutscene_data.h. Do not edit by hand. */
/* Deliberately does NOT include the original header: that header's
 * `extern const T name[..];` declarations would collide with the
 * `#define name (*p_name)` macros below if both ever appear in the
 * same translation unit. Instead this reproduces the *other*
 * includes the original header needed (for types/macros like
 * OAM_DATA_SIZE), and is meant to be used INSTEAD of the original
 * in any 3DS-port translation unit. */
#ifndef PORT_GEN_IN_GAME_CUTSCENE_DATA_ROM_H
#define PORT_GEN_IN_GAME_CUTSCENE_DATA_ROM_H
#include "types.h"
#include "structs/in_game_cutscene.h"
#include "constants/in_game_cutscene.h"

extern const struct InGameCutsceneData(*p_sInGameCutsceneData)[IGC_COUNT];
#define sInGameCutsceneData (*p_sInGameCutsceneData)

void PortGen_in_game_cutscene_data_Init(void);

#endif /* PORT_GEN_IN_GAME_CUTSCENE_DATA_ROM_H */
