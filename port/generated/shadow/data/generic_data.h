/* Generated by tools/gen_port_rom_offsets.py from include/data/generic_data.h. Do not edit by hand. */
/* Deliberately does NOT include the original header: that header's
 * `extern const T name[..];` declarations would collide with the
 * `#define name (*p_name)` macros below if both ever appear in the
 * same translation unit. Instead this reproduces the *other*
 * includes the original header needed (for types/macros like
 * OAM_DATA_SIZE), and is meant to be used INSTEAD of the original
 * in any 3DS-port translation unit. */
#ifndef PORT_GEN_GENERIC_DATA_ROM_H
#define PORT_GEN_GENERIC_DATA_ROM_H
#include "types.h"
#include "macros.h"

extern const s16(*p_sSineTable)[2*PI+PI_2];
#define sSineTable (*p_sSineTable)
extern const u8(*p_sRandomNumberTable)[256];
#define sRandomNumberTable (*p_sRandomNumberTable)
extern const Func_T(*p_sIntrTable)[13];
#define sIntrTable (*p_sIntrTable)

void PortGen_generic_data_Init(void);

#endif /* PORT_GEN_GENERIC_DATA_ROM_H */
