/* Generated by tools/gen_port_rom_offsets.py from include/data/cutscenes/cutscenes_data.h. Do not edit by hand. */
/* Deliberately does NOT include the original header: that header's
 * `extern const T name[..];` declarations would collide with the
 * `#define name (*p_name)` macros below if both ever appear in the
 * same translation unit. Instead this reproduces the *other*
 * includes the original header needed (for types/macros like
 * OAM_DATA_SIZE), and is meant to be used INSTEAD of the original
 * in any 3DS-port translation unit. */
#ifndef PORT_GEN_CUTSCENES_DATA_ROM_H
#define PORT_GEN_CUTSCENES_DATA_ROM_H
#include "types.h"
#include "macros.h"
#include "constants/cutscene.h"
#include "structs/cutscene.h"

#define CUTSCENES_DATA_H

extern const struct CutsceneInfo(*p_sCutsceneData)[CUTSCENE_COUNT];
#define sCutsceneData (*p_sCutsceneData)

void PortGen_cutscenes_data_Init(void);

#endif /* PORT_GEN_CUTSCENES_DATA_ROM_H */
