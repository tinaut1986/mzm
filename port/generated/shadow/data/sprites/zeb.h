/* Generated by tools/gen_port_rom_offsets.py from include/data/sprites/zeb.h. Do not edit by hand. */
/* Deliberately does NOT include the original header: that header's
 * `extern const T name[..];` declarations would collide with the
 * `#define name (*p_name)` macros below if both ever appear in the
 * same translation unit. Instead this reproduces the *other*
 * includes the original header needed (for types/macros like
 * OAM_DATA_SIZE), and is meant to be used INSTEAD of the original
 * in any 3DS-port translation unit. */
#ifndef PORT_GEN_ZEB_ROM_H
#define PORT_GEN_ZEB_ROM_H
#include "types.h"
#include "oam.h"

#define ZEB_DATA_H

extern const u32(*p_sZebPinkGfx)[162];
#define sZebPinkGfx (*p_sZebPinkGfx)
extern const u16(*p_sZebPinkPal)[16];
#define sZebPinkPal (*p_sZebPinkPal)
extern const u32(*p_sZebBlueGfx)[162];
#define sZebBlueGfx (*p_sZebBlueGfx)
extern const u16(*p_sZebBluePal)[16];
#define sZebBluePal (*p_sZebBluePal)
extern const struct FrameData(*p_sZebOam_Idle)[5];
#define sZebOam_Idle (*p_sZebOam_Idle)
extern const struct FrameData(*p_sZebOam_Moving)[5];
#define sZebOam_Moving (*p_sZebOam_Moving)

void PortGen_zeb_Init(void);

#endif /* PORT_GEN_ZEB_ROM_H */
