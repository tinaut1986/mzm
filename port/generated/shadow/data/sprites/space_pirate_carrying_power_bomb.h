/* Generated by tools/gen_port_rom_offsets.py from include/data/sprites/space_pirate_carrying_power_bomb.h. Do not edit by hand. */
/* Deliberately does NOT include the original header: that header's
 * `extern const T name[..];` declarations would collide with the
 * `#define name (*p_name)` macros below if both ever appear in the
 * same translation unit. Instead this reproduces the *other*
 * includes the original header needed (for types/macros like
 * OAM_DATA_SIZE), and is meant to be used INSTEAD of the original
 * in any 3DS-port translation unit. */
#ifndef PORT_GEN_SPACE_PIRATE_CARRYING_POWER_BOMB_ROM_H
#define PORT_GEN_SPACE_PIRATE_CARRYING_POWER_BOMB_ROM_H
#include "types.h"
#include "macros.h"
#include "oam.h"

#define SPACE_PIRATE_CARRYING_POWER_BOMB_DATA_H

extern const u32(*p_sFakePowerBombGfx)[115];
#define sFakePowerBombGfx (*p_sFakePowerBombGfx)
extern const u16(*p_sFakePowerBombPal)[16];
#define sFakePowerBombPal (*p_sFakePowerBombPal)
extern const struct FrameData(*p_sFakePowerBombOam_Idle)[5];
#define sFakePowerBombOam_Idle (*p_sFakePowerBombOam_Idle)

void PortGen_space_pirate_carrying_power_bomb_Init(void);

#endif /* PORT_GEN_SPACE_PIRATE_CARRYING_POWER_BOMB_ROM_H */
