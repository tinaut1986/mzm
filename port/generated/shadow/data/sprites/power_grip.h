/* Generated by tools/gen_port_rom_offsets.py from include/data/sprites/power_grip.h. Do not edit by hand. */
/* Deliberately does NOT include the original header: that header's
 * `extern const T name[..];` declarations would collide with the
 * `#define name (*p_name)` macros below if both ever appear in the
 * same translation unit. Instead this reproduces the *other*
 * includes the original header needed (for types/macros like
 * OAM_DATA_SIZE), and is meant to be used INSTEAD of the original
 * in any 3DS-port translation unit. */
#ifndef PORT_GEN_POWER_GRIP_ROM_H
#define PORT_GEN_POWER_GRIP_ROM_H
#include "types.h"
#include "oam.h"

#define POWER_GRIP_DATA_H

extern const u32(*p_sPowerGripGfx)[279];
#define sPowerGripGfx (*p_sPowerGripGfx)
extern const u16(*p_sPowerGripPal)[32];
#define sPowerGripPal (*p_sPowerGripPal)
extern const struct FrameData(*p_sPowerGripOam_Idle)[5];
#define sPowerGripOam_Idle (*p_sPowerGripOam_Idle)
extern const struct FrameData(*p_sPowerGripGlowOam_Idle)[15];
#define sPowerGripGlowOam_Idle (*p_sPowerGripGlowOam_Idle)

void PortGen_power_grip_Init(void);

#endif /* PORT_GEN_POWER_GRIP_ROM_H */
