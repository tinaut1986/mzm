/* Generated by tools/gen_port_rom_offsets.py from include/data/sprites/multiviola.h. Do not edit by hand. */
/* Deliberately does NOT include the original header: that header's
 * `extern const T name[..];` declarations would collide with the
 * `#define name (*p_name)` macros below if both ever appear in the
 * same translation unit. Instead this reproduces the *other*
 * includes the original header needed (for types/macros like
 * OAM_DATA_SIZE), and is meant to be used INSTEAD of the original
 * in any 3DS-port translation unit. */
#ifndef PORT_GEN_MULTIVIOLA_ROM_H
#define PORT_GEN_MULTIVIOLA_ROM_H
#include "types.h"
#include "oam.h"

#define MULTIVIOLA_DATA_H

extern const u32(*p_sMultiviolaGfx)[221];
#define sMultiviolaGfx (*p_sMultiviolaGfx)
extern const u16(*p_sMultiviolaPal)[16];
#define sMultiviolaPal (*p_sMultiviolaPal)
extern const struct FrameData(*p_sMultiviolaOam_Moving)[57];
#define sMultiviolaOam_Moving (*p_sMultiviolaOam_Moving)

void PortGen_multiviola_Init(void);

#endif /* PORT_GEN_MULTIVIOLA_ROM_H */
