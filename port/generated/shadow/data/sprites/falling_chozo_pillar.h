/* Generated by tools/gen_port_rom_offsets.py from include/data/sprites/falling_chozo_pillar.h. Do not edit by hand. */
/* Deliberately does NOT include the original header: that header's
 * `extern const T name[..];` declarations would collide with the
 * `#define name (*p_name)` macros below if both ever appear in the
 * same translation unit. Instead this reproduces the *other*
 * includes the original header needed (for types/macros like
 * OAM_DATA_SIZE), and is meant to be used INSTEAD of the original
 * in any 3DS-port translation unit. */
#ifndef PORT_GEN_FALLING_CHOZO_PILLAR_ROM_H
#define PORT_GEN_FALLING_CHOZO_PILLAR_ROM_H
#include "types.h"
#include "oam.h"

#define FALLING_CHOZO_PILLAR_DATA_H

extern const u32(*p_sFallingChozoPillarGfx)[219];
#define sFallingChozoPillarGfx (*p_sFallingChozoPillarGfx)
extern const u16(*p_sFallingChozoPillarPal)[16];
#define sFallingChozoPillarPal (*p_sFallingChozoPillarPal)
extern const struct FrameData(*p_sFallingChozoPillarOam_Falling)[2];
#define sFallingChozoPillarOam_Falling (*p_sFallingChozoPillarOam_Falling)

void PortGen_falling_chozo_pillar_Init(void);

#endif /* PORT_GEN_FALLING_CHOZO_PILLAR_ROM_H */
