/* Generated by tools/gen_port_rom_offsets.py from include/data/sprites/waver.h. Do not edit by hand. */
/* Deliberately does NOT include the original header: that header's
 * `extern const T name[..];` declarations would collide with the
 * `#define name (*p_name)` macros below if both ever appear in the
 * same translation unit. Instead this reproduces the *other*
 * includes the original header needed (for types/macros like
 * OAM_DATA_SIZE), and is meant to be used INSTEAD of the original
 * in any 3DS-port translation unit. */
#ifndef PORT_GEN_WAVER_ROM_H
#define PORT_GEN_WAVER_ROM_H
#include "types.h"
#include "oam.h"

#define WAVER_DATA_H

extern const u32(*p_sWaverGfx)[289];
#define sWaverGfx (*p_sWaverGfx)
extern const u16(*p_sWaverPal)[16];
#define sWaverPal (*p_sWaverPal)
extern const struct FrameData(*p_sWaverOam)[7];
#define sWaverOam (*p_sWaverOam)

void PortGen_waver_Init(void);

#endif /* PORT_GEN_WAVER_ROM_H */
