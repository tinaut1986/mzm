/* Generated by tools/gen_port_rom_offsets.py from include/data/sprites/elevator_pad.h. Do not edit by hand. */
/* Deliberately does NOT include the original header: that header's
 * `extern const T name[..];` declarations would collide with the
 * `#define name (*p_name)` macros below if both ever appear in the
 * same translation unit. Instead this reproduces the *other*
 * includes the original header needed (for types/macros like
 * OAM_DATA_SIZE), and is meant to be used INSTEAD of the original
 * in any 3DS-port translation unit. */
#ifndef PORT_GEN_ELEVATOR_PAD_ROM_H
#define PORT_GEN_ELEVATOR_PAD_ROM_H
#include "types.h"
#include "oam.h"

#define ELEVATOR_PAD_DATA_H

extern const u32(*p_sElevatorPadGfx)[136];
#define sElevatorPadGfx (*p_sElevatorPadGfx)
extern const u16(*p_sElevatorPadPal)[16];
#define sElevatorPadPal (*p_sElevatorPadPal)
extern const struct FrameData(*p_sElevatorPadOam_Moving)[5];
#define sElevatorPadOam_Moving (*p_sElevatorPadOam_Moving)
extern const struct FrameData(*p_sElevatorPadOam_Idle)[7];
#define sElevatorPadOam_Idle (*p_sElevatorPadOam_Idle)

void PortGen_elevator_pad_Init(void);

#endif /* PORT_GEN_ELEVATOR_PAD_ROM_H */
