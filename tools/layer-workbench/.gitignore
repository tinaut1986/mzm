# Generados por build_maps.py / build_sprites.py -- se reconstruyen en segundos
maps.json
maps.json.gz
sprites.json
thumbs/

# Built by wasm/build.sh from the real port_stereo_depth.c / port_layer_fixes.c.
# Needs emcc, which is not guaranteed to be on every machine that opens the
# workbench, so it is generated on demand (serve.py) rather than checked in.
depth_engine.js

# Local Emscripten toolchain, bootstrapped on demand by wasm/setup_emsdk.bat
# (or setup_emsdk.sh) when emcc is not already on PATH.
wasm/emsdk/

__pycache__/

# El .bin de prueba lo hace make_test_rec.py
mzm-rec-test.bin
